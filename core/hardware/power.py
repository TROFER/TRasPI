class Power:

    @classmethod
    def halt():
        print("WOULD HALT")
        # core.render.renderer.close()
        # core.Hardware.Display.clear()
        # os.system("halt")

    @classmethod
    def restart():
        print("WOULD REBOOT")
        # core.render.renderer.close()
        # core.Hardware.Display.clear()
        # os.system("reboot")
