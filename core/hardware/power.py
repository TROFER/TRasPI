class PowerControls:

    @classmethod
    def halt():
        print("WOULD HALT")
        # core.render.renderer.close()
        # core.Hardware.DisplayFunctions.clear()
        # os.system("halt")

    @classmethod
    def restart():
        print("WOULD REBOOT")
        # core.render.renderer.close()
        # core.Hardware.DisplayFunctions.clear()
        # os.system("reboot")
