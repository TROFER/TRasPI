class Power:

    @classmethod
    def halt(cls):
        print("WOULD HALT")
        # core.render.renderer.close()
        # core.Hardware.Display.clear()
        # os.system("halt")

    @classmethod
    def restart(cls):
        print("WOULD REBOOT")
        # core.render.renderer.close()
        # core.Hardware.Display.clear()
        # os.system("reboot")
