This program requires the installation of Music Player Daemon

To install use:
apt-get update
apt-get install mpd mpc

Trouble Shooting:
Access Denied / ALSA initialisation error:
  *Disable and re-enable ALSA in 'dietpi-config' / audio
  *Make sure device is set to the 3.5 audio jack

