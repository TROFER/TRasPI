import core
import os
from app import App

class Index:

    DEFAULT_PATH = f"{core.sys.const.path}user/music"

    def scan(self):
        for directories in App.const.directories:
            for _genre in os.




































